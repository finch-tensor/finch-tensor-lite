def compute(x: int64, y: int64) -> int64:
    temp: int64 = x
    temp: int64 = finchlite.ffunc.add(temp, y)
    return temp

def main() -> int64:
    a: int64 = 10
    b: int64 = a
    c: int64 = b
    d: int64 = 5
    if finchlite.ffunc.gt(c, 5):
        a: int64 = finchlite.ffunc.add(a, d)
        b: int64 = a
    result: int64 = 0
    for i in range(0, 5):
        temp: int64 = i
        result: int64 = finchlite.ffunc.add(result, temp)
        if finchlite.ffunc.eq(finchlite.ffunc.mod(i, 2), 0):
            result: int64 = finchlite.ffunc.mul(result, 2)
    if finchlite.ffunc.gt(result, 20):
        c: int64 = result
        result: int64 = finchlite.ffunc.add(c, b)
    else:
        c: int64 = b
        result: int64 = finchlite.ffunc.mul(c, 3)
    for i in range(0, 3):
        a: int64 = i
        for j in range(0, 2):
            b: int64 = j
            result: int64 = finchlite.ffunc.add(result, finchlite.ffunc.add(a, b))
    d: int64 = result
    if finchlite.ffunc.lt(d, 100):
        c: int64 = d
        if finchlite.ffunc.gt(c, 50):
            result: int64 = finchlite.ffunc.mul(c, 2)
        else:
            result: int64 = c
    else:
        result: int64 = d
    a: int64 = result
    b: int64 = a
    return b

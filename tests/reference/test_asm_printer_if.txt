def if_else() -> int64:
    a: int64 = 5
    if finchlite.ffunc.eq(a, 5):
        a: int64 = finchlite.ffunc.add(a, 10)
    if finchlite.ffunc.lt(a, 15):
        a: int64 = finchlite.ffunc.sub(a, 3)
    else:
        a: int64 = finchlite.ffunc.mul(a, 2)
    return a
